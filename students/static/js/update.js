/*function validateForm(event) {
    let idInput = document.querySelector("[name = 'ID']" );   
    let department = document.querySelector("select").value;
    let formIsValid = true;

 /*   
    if (idInput.value.length !== 8) {
      alert("ID must be exactly 8 characters long.");
      formIsValid = false;
      
    }
   
    // Clear previous error messages
    errorField.textContent = '';
    const id = idInput.value;
    if (id.length !== 14) {
        errorField.textContent = 'National ID must be 14 digits!';
        return false; // Prevent form submission
    } 
    if (department === "") {
        alert("Please select a department.");
        formIsValid = false;
    }


    if (formIsValid === false)
    {
        event.preventDefault();

    }
  }*/